To run the project 
cd project folder path ( "c: ...... \DADV-Project\bangalore_intelligence")
create a virtual env to install all dependencies instead of global install 
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run app.py
